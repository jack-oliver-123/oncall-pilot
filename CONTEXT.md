# 项目领域词汇

本文件只记录跨 Change 稳定复用的概念及其关系，不记录实现细节、命令、路径或当前变更状态。

## On-call Pilot

本项目及其产品的统一名称。避免使用 Super AI、super_ai 或 Oncall Pilot 等其他名称指代本项目或产品。

## 值班人员

使用 On-call Pilot 处理和协调日常值班工作的主要参与者。他们根据待处理事项、影响、状态、负责人和下一步采取行动，无需理解系统的技术实现。

## Change

一次可追踪的需求变更及其正式 OpenSpec artifact 集合。Change 有明确的目标、范围、验收边界和生命周期；相关工作不得在多个 Change 之间静默合并。

## Artifact

Change 生命周期中用于表达正式意图的持久化文档。Artifact 共同构成该 Change 的可恢复契约，不等同于探索笔记或会话记忆。

## 控制平面

负责管理正式变更状态、artifact 依赖、实现授权、验证和归档的协作层。在本项目中，OpenSpec 是 Change 的控制平面。

## 方法层

提供探索、质询、研究、领域建模、原型、测试、诊断或审查方法的协作层。方法层可以发现问题和提出建议，但不拥有 Change 的正式状态。

## 组合工作流

将控制平面与方法层按职责连接起来的协作方式：OpenSpec 冻结并追踪正式意图，方法层按问题类型提供辅助能力，用户确认负责把候选结论提升为正式约束。

## 阶段门禁

要求在高影响生命周期节点明确确认后才能继续的协作边界。本项目的三个门禁是范围冻结、实现授权和验证后归档。

## 正式事实源

当前 Change 中已确认并持久化的 OpenSpec 内容，包括目标、需求、设计、任务和验证状态。它是实现阶段必须遵循的契约；方法层建议不能静默覆盖它。

## 领域词汇

对项目参与者、行为、边界或关系的稳定命名。跨 Change 复用且含义稳定的领域词汇进入本文件；仅服务于单次 Change 的假设和限定含义留在该 Change 中。
