global:
  resolve_timeout: 5m

route:
  receiver: local
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

# 本地保留告警状态；后续 Change 接入真实通知，不预置未实现的 webhook。
receivers:
  - name: local
